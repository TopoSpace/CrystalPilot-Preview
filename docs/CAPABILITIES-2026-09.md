# CrystalPilot 能力边界（2026-09，第二轮逐轮更新）

> 口径：只写**现在的代码实际算什么、用什么判据、明确不算什么**。所有数字是代码里的判据默认值，不是真理。
> 每轮结束更新对应行的"状态"，并在 §8 追加一条变更记录。缺陷编号（D1…）沿用第二轮升级计划里的编号。
> 图例：「有」= 有且验证过；「部分」= 能算但有明确缺口；「无」= 没有代码；「有误」= 已知算错（已上线）
> 2026-09-05 追加盘点见 `AUDIT-2026-09-05.md`，下一轮方案见 `PLAN-2026-09-05-upgrade.md`。下文各轮测试数量是对应历史版本记录，不是本轮全套测试的新结果；本轮已清理 LCD/PLD、堆积、CShM 和 Systre 的明显旧条目冲突。

---

## 1. 结构分析（对照师兄清单）

| 能力 | 状态 | 现在算什么 | 判据 / 来源 | 不算什么 · 已知问题 | 代码 |
|---|---|---|---|---|---|
| 成键（连通表） | 有 R2 | **一份真值**：`chem/bonding.py` = smtbx `connectivity_table` 引擎（原生 PART 语义、对称精确跨像对）+ 一个分类后处理器；每条边带 kind（covalent / coordination / eta / metal_metal / non_bonded_close）与决定它的判据串 | 判定序：金属–金属 ≤ Σr_cov+0.4；η 环（`connectivity._eta_rings`）；金属–H：已有重原子伙伴的 H 永不是配体，端基氢化物 ≤ Σr_cov+0.15；金属–C：螯合咬合（C 经更短的 M–X 键 1-3）→ non_bonded_close，否则 ≤ Σr_cov 为 σ 键；金属–给体按 `MetalProfile` 窗口（缺表金属 Σr_cov+0.45，判据串写明）；非金属 ≤ Σr_cov+0.45；金属判定 = 周期表白名单（未知符号永不为金属） | 六个消费者全部迁移（inspect/get_geometry、精修用的金属接触修剪、报告渲染器、查看器场景、`analyze_connectivity`、前端配位表），`tests/test_bonding_status_quo.py` 的 MIGRATION LOG 记录了每一步移动的数字；**未做**：远小于共价和的"不可能接触"仍按窗口分类（`analyze_connectivity` 靠 short_factor 扫描剔除并另列）；离线评分器 `benchmark/grade.py` 仍用旧 `_bond_cutoff`（未动） | `crystalpilot/chem/bonding.py`；`tests/test_bonding.py`（46）；`tests/test_bonding_status_quo.py`（16） |
| 配位数 / 几何指数 | 有 R2/R4 | CN = **配体数**：η 环计一、金属–金属接触另列不计、不可能接触另列；三态 `cn_plausible` 区分通过/不通过/未检查；τ₄/τ₅、多面体；前端读取服务端成键分类 | 元素窗口来自 `METAL_PROFILES`；CN 4–6 的 CShM 已实现，见本表专行 | CN 7/8、η 配体的 CShM 尚不支持；`metal_bonded_audit` 的 cn_sites 约定与 inspect 的配体数约定并存，须按各自说明理解 | `chem\bonding.py::BondTable.cn`；`chem\knowledge.py::cn_status`；`ui\src\workbench\crystal\CoordinationSection.tsx` |
| 氢键 | 有 R2/R5 | D–H···A 的距离/角度/对称码；PART、H 来源、边界与光环不足标志；无 H 时明确降级为 D···A 几何候选；显示关系随实例范围扩展 | Olex2 与 Steiner/PLATON 两套判据随结果回显；骑乘 H 与实验 H 几何的差别须保留 | `analyze_packing` 已给 HTAB/EQIV 建议，`run_shelxl(extra_cards=...)` 计算带 esd 的 CIF 表，历史 reg10 有真实交付证据。查看器/网格几何不自行产生 esd；候选不等于相互作用能 | `chem\interactions.py`；`refine\shelx_cards.py`；`tests\test_known_answers_interactions.py` |
| 短接触 | 部分 | 查看器"短接触"层：非 H 原子对，d ≤ Σr_vdW（缺表 2.0 Å），搜索半径 4.6 Å——泛泛的堆积邻居 | 排除氢原子；C–H···X / C–H···π / 卤键 / π–π 各有自己的图层（R2.3），提示文案已改实（D5 关闭） | 仅几何，不判类型 | `refine/scene.py::_contact_sym_table` |
| π–π 堆积 | 有 R3-R5 几何与环覆盖均已验证 | 芳香环间质心距、法线夹角、两侧垂距/滑移；对称唯一表与显示表共用引擎，已带 intra 标记；显示层按实例范围求关系；**穿对称元素闭合的环（反演中心 / 三次轴上的苯环）按闭合算符的幂真正建出**，环行带 `through_symmetry {op, order, asu_atoms}`，参与每一条 π–π / C–H···π | Olex2 pipi 默认距离 4.0 Å、夹角 30°、滑移 3.0 Å；判据不是作用能证明；环闭合规则：闭合算符非恒等且阶有限、路径长 × 阶落在 5/6 元 | `find_rings` 仍只搜 5/6 元环，8 元并环的堆积行豁免（Zn2_dhtp 8 行）；PLATON 交叉比对 18 行（Zn2_dhtp 12 + 5 + COD 2232050 1），最少比对行数写进测试 | `chem\interactions.py::_rings_through_symmetry/_scan_pipi`；`tests/test_interactions.py`；§7b |
| C–H···π / C–H···X / 卤键 / 阴离子–π | 有 R2 | 同一引擎：H···环心 ≤ 3.2 Å、∠C–H···Cg ≥ 120°、法向投影落在环半径 +0.5 内（Nishio）；H···A ≤ Σr_vdW、∠ ≥ 120°，A ∈ O/N/F/Cl/S；X ∈ Cl/Br/I，X···Y ≤ Σr_vdW、∠C–X···Y ≥ 155°（IUPAC 2013）；阴离子（`_FRAGMENT_SIGNATURES` 带负电荷的签名）质心到环心 ≤ 4.5 Å、偏移在环半径内，标"弱读数" | 行按距离筛出、`passes` = 距离且角度；每行带对称码、`boundary`；环自身的 C–H 不算 C–H···π，共用原子的稠环不算 π–π | 无 esd（只有 SHELXL 有）；每类显示上限 400 行（`truncated` 报告） | `chem/interactions.py`；查看器六个图层药丸（氢键 / π–π / C–H···π / C–H···X / 卤键 / 阴离子–π），点击虚线看几何 + 判据 + 引用 |
| 孔道体积 / 电子数 / 维度 / LCD / PLD | 有 已实现并有已知答案对照 | smtbx 掩膜给体积/电子数；周期网格给维度与方向；距离场给 LCD/球心，渗流二分给 PLD。贯穿孔不报伪质心，孤立腔的 PLD 为 null。单胞量不随超胞画面倍增 | `VOIDS_CACHE_V=7`：LCD 分数晶格分箱与 Lipschitz 精化，`lcd_exact` 说明格点最大值状态；PLD 对所有网格路径求渗流阈值，超过体素预算则粗化并报告步长；**R3-R5：每个贯通孔另给沿 a / b / c 的 PLD（`pld_along`，不沿该轴贯通 = null，全路径 PLD 超 15 s 则跳过并说明）**，几何（纯 CIF）孔道路径同样带全路径与沿轴 PLD；`analyze_packing.pores.electron_count_basis` 把本节点重算的电子数与精修掩膜快照并列（同为每胞，差 >20 % 时写明以快照为准）；**R3-R7 修复后**重算走 `BypassMask` 并记 `bypass{converged,…}` 与 `anomalous_terms`，重算未收敛就不与快照比（`recomputed_converged`）；已有解析解与真实 MOF 对照 | 网格尺度不是实验 esd，不能冒充解析 Voronoi 结果。沿轴 PLD 只有三个晶轴方向，非轴向通道只在 `pld_directions` 里出现；~~Zr-MOF n0109 重算 1193.2 vs 快照 591.6 e/胞 的 2.02 倍来源未追~~ → 已追到并修复（重建会话丢反常散射项，§3 掩膜行）：n0108 重算 593.7 vs 快照 591.6；n0109 419.3 vs 591.6 是真的模型不同（SHELXL 之后）。真实 MOF 冷分析的孔道块耗 13–17 s（n0109）。旧贯穿孔质心已停用 | `chem\pores.py`；`refine\scene.py::build_void_ccp4`；`tests\test_pores.py`；§7b |
| 堆积数字 | 有 R3.3 | **Kitaigorodskii 堆积指数** = vdW 球**并集**体积 / V_cell × 100（晶胞网格上按 min_a(\|x−r_a\|−r_vdW(a)) < 0 计数，相交只计一次，误差 = 与两倍粗网格的差）；`vdw_sum_pct` = 球体积直接求和（无重叠修正，只作对照）；Å³/非氢原子 = V_cell / Σocc(非氢)，对照 ≈18 Å³ 的经验值；三者与溶剂可及体积分开报，**不互为补数**；随 voids.json（v4）一起出，每个节点都有，分析页签显示 ± | vdW 半径取 cctbx 表（缺表 2.0 Å 并注明）；求和按占有率加权，并集按全占计入（无序对在空间上本就重叠）；网格上限 40 万点（步长随之变大并报告）；实测：cage n0200 并集 51.4 ± 0.15 %（直接求和 96.4 %，溶剂可及 29.3 %），hex n0041 18.4 ± 0.14 %（38.8 %，78.5 %），直接求和在致密结构上会给出 96 % 这种没人认得的"堆积指数"，所以标题数字是并集 | 探针滚不进的尖角空间可占胞体积 ~30% 而溶剂可及体积为零（知识库 §5.7），输出随附；无 esd；PLD + 堆积数字合计只加 ~3 s，voids.json 的 70 s（hex）是 smtbx 掩膜与电子积分本来的开销 | `chem/pores.py::packing_index`、`_vdw_union` |
| 阴离子 / 客体位置 | 有 R3.4 | `chem/guests.py`：宿主掩膜**只用宿主原子**（客体会填满自己的孔，全模型掩膜看不到它），无需反射数据；每个客体片段给出 site ∈ 笼内 / 通道 / 分子间空腔 / 晶格间隙、孔编号、宿主片段、到内切球心距离、最近宿主接触（带对称码）、间隙 clearance、是否跨区域；四条定义逐字随输出 | 宿主 = `role==main` 片段 + 维度 ≥1 的片段 + 原子数 ≥ 最大宿主 50% 的片段（第二个独立的笼是宿主）；笼内 = 区域壁面原子 ≥95% 属同一个 0-D 宿主片段**实例**（按实例不按类型：穿过一叠大环拷贝的通道不是笼）；通道 = 区域维度 ≥1；空腔 = 0-D 且不能归于单一分子；间隙 = 质心不在任何 ≥ 8 Å³ 的区域里（1.2 Å 探针滚不进）；胞角上的腔先折回再读壁面（D17 的下一层）；`void_membership` 作为解析交叉核对 | 分子盐里"宿主/客体"只是大小规则（dbu：18 原子的阴离子对 37 原子的阳离子 = 49% → 客体，"在通道中"）；跨两个区域的客体按质心归属并标 straddles；PART 无序的两个组分是两个客体记录；特殊位置上的对称客体质心只是 ASU 半个分子的；无 esd | `chem/guests.py`；`tests/test_guests.py`（35：pcu 通道、槽缝间隙、C60 壳笼、P-1 胶囊空腔、去溶剂宿主、PART、宿主规则）；分析页签 |
| 骨架维度 / 独立网 / 互穿 / 互锁 | 有 R3-R5 环穿越判定 | 去客体后的周期分量、维度与方向；`symmetry_related` / `symmetry_related_1d` 表示逐原子平移或空间群映射；**互穿由环穿越判定**（`chem/threading.py`）：每个网按金属簇–连接子（无金属时分支点）收缩，取每个角的最小环（≤12 节点、Franzblau 最短路环）作窗口，窗口用质心扇面张成，另一网的原子级键穿过扇面即穿越；`interpenetrated`（维度 ≥2 的网对）/ `interlocked_1d`（一维链对）为 true / false，`interpenetration_status` = tested / inconclusive / not_testable / not_applicable，`threading.pairs` 带窗口数、被穿窗口数与一例 | 带平移边的 BFS 与失配秩；元素感知映射容差 0.02 分数坐标；最多比较 12 网、256 候选平移；穿越检验预算 20 s | 对称相关不证明互穿（平行层反例已是已知答案：不互穿）；预算内未完成 → null 并说明；链的收缩图无环 → not_testable；棒状 SBU → inconclusive；没有 Gauss 链接数。已知答案：MOF-5 + 平移拷贝 / 反演相关双重互穿 MOF-5 → true，平行层 → false，pcu 3 个四元窗口、dia 12 个六元窗口 | `chem\topology.py`；`tests\test_topology.py`；`tests\test_known_answers_topology.py` |
| 节点–连接子简化网、RCSR 符号 | 有 R4/已知答案校验 | 金属簇–连接子收缩、带晶格平移的边、连接数直方图、CGD 导出；Systre 返回每个连通分量的符号与错误状态；查看器按 tiles 显示简化网 | **Systre 19.6.0 已安装并经真实二进制及文献对照**，见 §7b；无 jar 的环境仍须报告未计算 | 收缩规则有适用前提，棒状 SBU 不能无条件视为点；COF/HOF 分支点简化、混金属簇成分等仍有限制；网符号不等于机械互锁证明 | `chem\topology.py`；`refine\analysis.py::topology_from_res`；`AnalysisPanel.tsx`；`CrystalViewer.tsx` |
| 螺旋链 | 有 R4 | `helices`：1-D 片段被空间群的螺旋操作（模晶格平移）映回自身；`screw n_m`、阶数、手性（右 / 左 / 2₁ 与外消旋堆积为 null）、螺距 = T·min(m, n−m)（3₂ 的螺距是 c 不是 2c，计划里 \|t\|×阶 的写法已在定义文本中改正）、轴重复 T | 算符读 `all_ops()` + `translation_part_info`，命名规则与 `scene._symmetry_elements` 相同（正向算符命名轴） | 中心对称群两手皆有 → `racemic: true` 而不选边；对称等价链只报一次；两条独立链若原子标签相同会合并（未见） | `chem/topology.py`；`tests/test_topology.py`（P3₁ 右、P3₂ 左、P2₁ null、R-3 外消旋）；真实晶体已知答案 COD 4115425（Pt→Ag 链，P6₁：6₁ 右手，螺距 = c = 41.608 Å；`tests/test_known_answers_topology.py`） |
| 配位多面体 CShM | 有 R4 | `chem/shape.py::cshm`：连续形状量度 S = 100·min_{置换, SO(3) 旋转, 尺度, 平移} Σ\|q−sRp\|²/Σ\|q−q̄\|²（Pinsky & Avnir 1998；参考形按 Alvarez 2005 / SHAPE，CN 4/5/6：T-4 SP-4 / TBPY-5 SPY-5 vOC-5 / OC-6 TPR-6），置换穷举 × Kabsch/SVD × 解析尺度，中心原子作为两组点集的额外顶点（SHAPE 惯例，`include_centre`）；`inspect_model` 的金属环境在 tau4/tau5 旁给 `cshm{}`、`closest_shape`、`closest_cshm` | 钉住文献常数：八面体→TPR-6 16.737、正方形→T-4 33.333（= 100/3，解析证明）、TBPY-5→SPY-5 5.375（参考角 104.9°，对该角敏感，已注明） | "最接近的参考形"是最小 S，不是分类；η 配体（顶点数 ≠ 配体数）与 ≥7 顶点不量、行内 `shape_note` 说明；螯合咬合角不加权（SHAPE 同）；CN 7/8 参考形未实现 | `chem/shape.py`；`refine/inspect.py::_coordination_shape`；`tests/test_shape.py`（40）、`tests/test_shape_wiring.py`（4） |
| 大环、笼、形状证据 | 有 R4（证据，不命名） | `largest_cycle`：一个有限片段无平移边分子图上的**最大无弦环**（萘 = 6 不是 10）与环空间维数，预算 DFS、截断如实报；`shape_evidence`：惯量主矩比 [I1/I3, I2/I3]、Wadell 球形度 (36πV²)^(1/3)/A（球 1.000、正八面体 0.8456，计划里的公式差 4^(2/3) 倍、"~0.7"是猜的，均已改正）、凸包体积/面积、最长轴、主轴框纵横比；分析产物对每个 0-D 片段（分子 / 笼 / 抗衡离子，按 ASU 标签集去重并计份数）报这两项 | 分子图由 BFS 偏移保证无平移边（连通过晶格平移的边会把链变成假大环）；`crystallographic_point_symmetry` 给把完整片段实例映回自身的空间群算符（模 1：算符只说类型与取向，不说位置） | **不做**（写进工具描述）：自动命名灯笼/梨型、有限分子的机械互锁（索烃/轮烷）、拓扑手性；点对称只对完整片段实例有意义，ASU 半分子给 order 1；大环预算 2×10⁵ 步（融合笼会截断并说明） | `chem/shape.py`；`refine/analysis.py::_finite_fragments`；实测 cage n0200：两个笼 91 / 77 原子、最大无弦环 8、球形度 0.81 / 0.83 |
| 对称元素 | 有 R2.2 | 空间群对称元素（带阶数、固有平移、命名）叠加层，按 `range.tiles` 平铺 | cctbx 空间群操作 | 预算：最多 8 个最靠中心的胞（每胞上限 240 个元素） | `refine/scene.py::_symmetry_elements`；`CrystalViewer.tsx` |
| 差值密度图 / Q 峰 | 有 R2.2 | 每个节点的 Fo−Fc / 2Fo−Fc 图、峰表；`inspect_map`、`interpret_peaks`；等值面与峰球按 `range.tiles` 平铺 | — | Q 峰球预算 3000 个（超出只画最靠中心的胞）；标签只在一个胞 | `CrystalViewer.tsx`（`tiledIsoCopies`） |
| 几何表 `get_geometry` | 有 R5 | 键长、键角（esd 从 SHELXL ACTA 拼接）；`scope=torsions` 二面角（跨对称算符、IUPAC 符号，esd 来自带 CONF 的 SHELXL 作业）；`planes` 环的最小二乘平面（rms、法线、质心）；`centroids` 环心；返回 `suggested_cards`（CONF / MPLA）供 `run_shelxl(extra_cards=)` | 环来自相互作用引擎的环定义（find_rings + 平面性）；SHELXL 算 esd | 平面 esd 只到 .lst（MPLA 无 CIF 目的地）；每类上限 400 行；无对称码扩展的键表 | `refine/tools_analysis.py`；`tests/test_geometry_scope.py`（6） |
| 量距（查看器） | 部分 | 2/3/4 点：距离、角、二面角 | 客户端几何 | 无 esd | `CrystalPane.tsx` |

## 2. 显示范围与叠加层是否跟随范围

| 叠加层 | 非对称单元 / 生长 | 晶胞 | 超胞 n≤4 | 说明 |
|---|---|---|---|---|
| 键、多面体、短接触 | 有 | 有 | 有 | 在实例原子集上算 |
| 相互作用（氢键 / π–π / C–H···π / C–H···X / 卤键 / 阴离子–π） | 有 | 有 | 有 | 在实例集 + 8 Å 光环上算，只报至少一端在范围内的行；范围外伙伴画到其位置并带对称码（R2.3） |
| 孔道等值面 | 有 | 有 | 有 | 按 `range.tiles` 平铺（R2.2）；tiles = 持有已画原子的胞 ∪ 堆积模式的 n³ 块，上限 64（超出取最靠中心的，药丸"图层覆盖 N / M 胞"） |
| 密度图、Q 峰 | 有 | 有 | 有 | 同上；Q 峰球预算 3000 |
| 对称元素 | 有 | 有 | 有 | 同上；预算 8 个胞 |
| 原子上限 | `MAX_ATOMS = 20000`，椭球上限 1500（超出退化为球棍并给"能力悬崖"药丸） | | | `refine/scene.py`；生长层数 `hops ≤ 4` |
| 切片种类（R1.3 起） | 非对称单元 / 生长 n 层（grow -s）/ **长满**（grow，R3-R5：直到同一片段里同一原子的同一旋转部分重复为止，0-D 分子长完整，链 / 层 / 网长一个周期并画帽原子，`grow_all {complete, periodic_edges, caps, n_added, budget_hit}` 如实回报，查看器药丸“周期截断 / 原子预算截断”）/ **补全**（grow -w：把已用算符施加到整个 ASU）/ 晶胞（pack cell）/ 超胞 n≤4 / **半径 pack**（pack r，1–30 Å，以选中原子或 ASU 质心为中心，整分子）/ **范围 pack**（按质心落在分数盒内，默认 −0.5…1.5）/ 装配（compaq -a，预填输入框交 agent） | | | `refine/scene.py::build_scene`（`_wrapped_groups`/`_pack_radius`/`_pack_range`）；`tests/test_scene_extent.py` 解析解 |

R2.2 已把"范围"做成场景契约（`range = {frac_lo, frac_hi, tiles, n_tiles, tiles_truncated, centre_cart}`，缓存键 `scene8_`）：按胞算的产物按节点缓存一次，只有便宜的实例化随范围变（`ui/src/lib/tiles.ts`）。

## 3. 晶体学工具层（73 注册 + 1 项目开关 = 74）

| 类别 | 工具 | 已知待办 |
|---|---|---|
| 数据 / 还原 / 帧 | import_frames, find_spots, index_frames, integrate_frames, scale_and_export, export_twin_hklf5, create_start_model, estimate_resolution, ingest_vendor_data, reduce_with_crysalis, import_cif_model, swap_reflection_data | `ingest_vendor_data` 导入时不报重复 hkl（reg8 待办） |
| 对称 / 定群 | check_symmetry, change_space_group, ncs_audit, screen_space_groups, reflection_statistics | `screen_space_groups` 候选列表不带"已试 / 已裁决" |
| 求解 | solve_charge_flipping, solve_superflip, interpret_peaks, run_shelxt | — |
| 建模 / 观察 | edit_atoms, add_atoms_from_difference_map, fourier_complete, fit_fragment, **search_fragment_pose / accept_fragment_pose**（R3-R4 WP5：整片段姿态搜索，峰三元组几何哈希 + 区域随机种子 × 构象库 × Nelder–Mead 6 自由度，碰撞场、锚定约束（元素通用：`to_element:"metal"`、on_peak、near_site）、特殊位置吸附与对称折叠，逐原子证据 direct_peak / weak_density / geometry_only，候选缓存按节点 + 修订号，预算 600 s；接受 = 一次成节点：共享 FVAR + PART + EADP；满足全部锚点的候选排在前面）, set_restraints, rename_atoms, assemble_asu, inspect_model, inspect_map, check_ligand, get_project_brief | `inspect_model` 的金属环境自 R4 起带 CShM（`cshm`/`closest_shape`，4–6 顶点的 σ 配位球；η 配体与 ≥7 顶点如实标未量）；`view_structure` 无原子时给晶胞框（R5，reg8） |
| 假设检验 | ghost_test, element_scan, probe_site | `element_scan(free_occupancy)` 疑失效；`ghost_test` 批次墙钟。**R3-R4 WP7**：这三个 + fit_fragment / search_fragment_pose / integrate_difference_density / audit_guest_evidence 的每次成功调用记入试验账本（`refine/trial_ledger.py`，键 = 工具 + 归一化输入），同节点同输入重跑附 `tool_status.prior_trial`、祖先附 `prior_trials_on_ancestors`，只提示不拒绝；`situation_report.open_items.recent_trials` |
| 无序 / 孪晶 | model_disorder, set_twin, invert_structure | `model_disorder` 分裂后不重派骑乘 H。**R3-R3 WP3**：PART 随原子走（`refine/parts.py`：删除清 `parts_extra` / 组成员，组空丢弃并重编 FVAR，分裂记录标 stale 后 undo 干净拒绝）；`edit_atoms(set_part/clear_part)` 显式且可 `model_disorder(undo=<label>)` 撤销；`preflight_restraints`（第 71 个工具）/ `set_restraints` / `run_shelxl` 报"请求 vs 实际施加"，跨非零 PART 的 DFIX/DANG/SADI/FLAT 点名（只警告；SHELXL 的这条规则未逐字核对手册） |
| 氢 | add_hydrogens | 金属键合载体默认跳过（应说明） |
| 精修 | refine, run_shelxl, run_olex2, optimize_weights, set_weights, set_resolution_limit, set_z | **R3-R3 WP2**：`run_shelxl(extra_cards=…)` 采纳后卡片成为有效模型状态（`flags.effective_cards` → `node.json.effective_state`、`model.res`），checkout 还原、后续 SHELXL / olex2 作业自动带入（失效卡按名拒绝作业）、`replace_cards=true` 换掉、删原子同命修剪；交付比对 final.res 与配对作业 job.ins 给 `restartable`。老节点没有这层（显示为空，不猜） |
| 掩膜 / 电子记账 | solvent_mask, integrate_difference_density | **R3-R3**：`solvent_mask` 的 `execution=ran` 与 `scientific_outcome`（未收敛 = inconclusive）分开；`situation_report` 的掩膜块改读 `solvent_mask_info`（T-k）；`audit_guest_evidence` 的分母带标签（working_occupancy / full_occupancy_model / full_occupancy_formula / formula_at_working_occupancy）、判词只读工作假设、检验 2 逐原子（钉边界 / 混合组 = inconclusive）、检验 3 报循环数与终止原因（0 循环永不 supports）、`conditioned_by_prior`（WP4）。**R3-R4-c**：检验 3 加位移对照（客体副本刚性移 0.3 Å 同法精修，`displacement_control.return_fraction` < 0.5 → inconclusive："不动"是惯性不是支持，Zr-MOF 复跑里 0.07 占有率客体曾被判 supports 而 SHELXL 让它漂 5 Å）。**R3-R7（2026-09-06）**：反常散射项随模型走，`io/shelx_writer.apply_anomalous_terms`（Sasaki，与精修引擎同一处）在每次会话重建（导入 / checkout / 分支 / 查看器重算）设 f′/f″；此前重建只设 `it1992` 表，Zr K 边数据（0.68883 Å，f′(Zr) = −9.04 e）上同一 `model.res` 的掩膜电子数 1193 vs 591（2.02 倍），pa1 hex n0024 "通道首轮密度为负" 也是它；`solvent_mask` 摘要 / `mask.info` 记 `anomalous_terms`；**收口点** `SolveSession.model` 属性在赋值时补项（SHELXL adopt / 改空间群 / 导入 / 拆分 / select 拷贝共 14 处一次覆盖）；BYPASS 发散护栏 `runaway_series` 加"已高于首轮估计"条件，下冲后回升的序列不再被判发散（R3-R5 复盘 §7） |
| 验证 / checkCIF | validate_structure, run_checkcif, submit_iucr_checkcif, audit_reflection_data, audit_element_assignment | 缺表金属 CN 静默通过（D9） |
| 分析 / 几何 / 视图 | get_geometry, **analyze_packing**（R5：读按节点缓存的分析产物，四块各带判据与解读，附 HTAB 卡；R4 加 `topology` 块：独立网/互穿、简化网 + RCSR 状态、螺旋、有限片段的大环与形状证据），audit_guest_evidence, audit_heavy_sites, view_structure, situation_report | `get_geometry` R5 起有二面角/平面/质心（`scope`）；`situation_report` R5 起带 `stage` 与只读的 `packing` 摘要（读该节点缓存的 analysis.json，未生成时报“未算”并指名 analyze_packing） |
| 节点树 | list_nodes, compare_nodes, branch, checkout | — |
| 交付 | write_outputs, finalize_delivery | **R3-R4 WP7**：diagnostic 封存自动把研究目标的未达层级与未试方向写进 `open_items`（REPORT.json.finalized.investigation + MANIFEST.json），既无 `set_investigation` 记录又无 `unmet_goals` 参数才拒绝；final 晋级不受此门 |
| 元数据 / 技能 / 目标 | set_experiment；list_skills, read_skill, save_skill, delete_skill；**set_investigation**（R3-R4 WP7：goal / candidate_complete·scientifically_established 两级（met 必须带证据）/ budget / ruled_out 带证据+节点 / open_directions，SESSIONLESS、不建节点；AGENTS v39 的"目标分级与停止规则"读它） | — |
| 子代理 | consult_specialist（项目设置 `enable_specialists` 打开才注册） | — |

守卫：每个注册工具必须出现在模板工具图谱、必须有 UI 卡、必须归入 MUTATING / READ_ONLY / SESSIONLESS 之一（`tests/test_agents_md.py`、`tests/test_skills.py`）；**R3-R3 WP8** 起 `run()` 里的运行时数值边界必须在 schema 声明 minimum / maximum（`tests/test_schema_bounds.py`，参数摘要行显示 `number[0.5..6]`）。**R3-R3 WP1** 起每个结果带 `summary.tool_status`（execution / scientific_outcome / state_changed / no_change / applicability / artifact_index；`tests/test_result_envelope.py`），UI 行首显示 无变化 / 未定论 / 证据相反 / 超时 芯片。模板 v37 正文 7915 字符（预算 9000；v35 加 `analyze_packing` 一行，v36 该行补"拓扑"，v37 加 `{delegation}` 槽：只在委派档位渲染的 v37-delegate 变体 8368 字符）。规格缓存按**内容**指纹（D15 关闭：touch / checkout / 拷贝不再触发冷启动，一字节改动仍失效）。每个工具描述末尾自动附一行参数摘要（`Params: a*: string; b: x|y = x; c: [string]`，必填 *、默认值、枚举；70 个工具合计 7.8k 字符，单行封顶 700），JSON schema 照旧原样下发，R5 针对“每格 15–33 次参数试探”。

## 4. Codex 内化（已存在，本轮只补齐）

| 环节 | 现状 |
|---|---|
| 内核 | `codex-home/config.toml`：**gpt-6-astra @ xhigh**（2026-09-05 本轮切换）；旧战役显式模型参数保留。推理档位 `EFFORT_CHOICES = low/medium/high/xhigh/max/ultra`（2026-09-05 R0 实测：网关接受 gpt-6-astra 的 `max`/`ultra` 真实回合，详见 `docs/reg1-2026-09-04/EFFORT-PROBE.md`；"最高档"判定 = xhigh 及以上，UI 显示 极高/最高/超高）。`approval_policy=on-request`，`sandbox=workspace-write`；MCP 服务器按项目用 argv 覆盖注入（`workbench/core.py`，`tool_timeout_sec=3900`）。**第二提供方（2026-09-06，主人给的临时测试渠道）**：`[model_providers.openrouter]`（OpenAI Responses 线、密钥在 git-ignored `secrets/openrouter.txt`，同一个 `print_token.py --cred` 钩子按需读取，网关提供方与密钥保留为默认）；项目设置 `model_provider_override` 只对**新线程**生效（provider 在 thread/start 固定，model/effort 每回合传）；`scripts/run_live.py --provider openrouter --model z-ai/glm-5.3 --effort high`。**按提供方的推理档位阶梯**：`EFFORT_LADDERS["openrouter"] = low/medium/high`（主人：GLM 用 high），阶梯没有 xhigh 时顶档同时充当 top_tier 与 aggressive 档，设置里的 `effort_choices / delegation.top_effort / aggressive_efforts` 随之变化，换提供方时无档可对应的 effort 自动落到该阶梯顶档。**视觉能力**：`workbench/model_caps.image_input_supported`（网关 = 多模态；OpenRouter 读 `GET /api/v1/models` 的 `input_modalities`，缓存一天；未知 = null），`settings.vision=false` 时 MCP 进程带 `CRYSTALPILOT_NO_IMAGES=1`，图片留在盘上给用户看（`images_saved`），不发给模型；模型切换翻转该能力时重建 app-server + MCP（运行中 409）。首格实证：z-ai/glm-5.3 收到 view_structure 的图片后 OpenRouter 404 "No endpoints found that support image input"，且图片留在历史里让该线程之后每回合都失败；第二格又被 Codex 自带的 `view_image` 工具塞图（Agent 读到"图片已存盘"就自己去打开），所以纯文本模型的线程在 thread/start 时带 `config.tools.view_image=false` 并在 developer 指令里写明（探针：模型答"没有 view_image 工具"）。第三个坑：codex 0.147 把全部 MCP 工具作为**一个** Responses API 命名空间工具（`mcp__crystalpilot`）发给模型，模型调用时必须回带 namespace；GLM-5.3 间歇性漏带 → Codex 回 "unsupported call: <tool>"（10:24 线程 4 次后放弃），0.147 无 chat 线路可退，所以非默认提供方的线程在 developer 指令里写明命名空间与重发规则，并要求"不要只宣布调用而不调用"（GLM 的另一个习惯）。Codex 自身的请求日志在 `codex-home/logs_2.sqlite`（表 logs，含每次 POST 正文），查"Agent 为什么不调工具"先看这里。**参数容错**：MCP 服务端关闭 SDK 校验，先把 schema 要 object/array 而模型发成 JSON 字符串的参数解码（GLM 把 `set_investigation.tiers` 双重编码四次后放弃账本），再用同一 jsonschema 校验，结果里带 `argument_notes` |
| 权限 | 四档 readonly / copilot / auto / full（`workbench/service.py:31-45`）；审批 = Codex elicitation → `_on_approval` 决策阶梯 → UI 停靠审批卡；`SHELL_APPROVAL_BLOCKLIST` 覆盖凭据、E 盘、kill、git push |
| 节点 / 分支 / 回滚 | 每次成功改模型调用自动提交节点（`nodes/nXXXX/model.res|cif|node.json|peaks.json|f_mask.pkl`）；`checkout` 恢复权重/尺度/限制/无序组/孪晶/掩膜快照/峰表/**反常散射项**（R3-R7）；`better_nodes()` 让"交付比树内最佳差"成为显式决定 |
| 交付 | `write_outputs`（四方对账、标签/元素门）→ `run_checkcif`（PLATON `-u`，alerts + KB 注释）→ `finalize_delivery`（waivers、MANIFEST 哈希） |
| 事件 | `core.py` 归一为 `agent_delta / tool_* / collab_* / turn_* / token_usage`，transcript 脱敏落盘。**事件身份（2026-09-05 R3-R1）**：每条落盘事件带 `eid` = transcript 行号（`_log_event` 盖章并返回；用户消息由 service 先落盘再推送，插话回显带同一 eid），SSE 从 transcript 快照的 `live_cursor` 续传，每条连接先发 `channel_hello{generation}`，通道重建（重开项目/重启服务器）时前端据此重新拉取而不是信任旧的 Last-Event-ID；transcript 接口分页（`before/limit` → `total/oldest_eid/has_more`），旧行按行号补 eid；命令完整输出落 `command_output/<item>.txt`（事件只留 1500 字符尾）；浏览器诊断 `POST /api/ui/diagnostics` → `workdir/ui-diagnostics.jsonl` |
| 阶段摘要 | 没有 harness 级抽象；UI 侧 `TurnDigest`/`WorkGroup`，agent 侧 `situation_report`/`get_project_brief`（R6 加阶段轨） |
| 重扩展模块的导入纪律（R5 P0） | numpy 与 scipy 各带一份 OpenBLAS，在 anyio 的 stdio worker 线程存在时**首次**导入会卡在 Windows loader lock：reg9-dbu 一格（2026-09-05）首次 `analyze_packing` 在 worker 线程的 `import scipy.spatial` 上停了 65 min（py-spy 证据在 `mcp/prewarm.py` 文档串），agent 按纪律没有杀进程、把它记为工具缺陷。修法：`mcp/server.py main()` 在 `anyio.run` 之前跑 `prewarm_preloop()`（numpy + scipy 全家），首次打开项目时在主线程 `prewarm_heavy_imports()`（cctbx / smtbx / iotbx / gemmi），app-server 启动预热同一份名单；`tests/test_prewarm.py` 用 ast 扫描 `crystalpilot/` 内所有函数体内的懒导入必须在名单上，并用冷进程 + stdio 真跑一次 `analyze_packing`（Ca-imidazolate）作回归。分析产物新增 `timings_s`（四块各自耗时）与构建期心跳 |

## 5. 子代理

| 路径 | 状态 |
|---|---|
| Codex 原生 multi_agent | 默认开，`[agents]` 限 2 线程 / 深度 1 / 1800 s；`collab_*` 事件已浮出；r23 实弹验证过 spawn/wait/send |
| 自研 `consult_specialist` | 五个只读专长；嵌套工作台 + `CRYSTALPILOT_MCP_READONLY=1` 服务端强制只读；结构化判词；项目开关 |
| 策略（主人 2026-09-04 拍板） | 默认关；只在最高推理档位的模板里提示"可并行委派只读审计"；R7 跑一格对照再定是否放宽 |
| R7 机制（2026-09-05，探针已验证） | 项目设置 `subagents`：`top_tier`（默认）/ `off`。有效推理档位 = 最高档（xhigh）且策略为 top_tier 时，AGENTS.md 换成 **v37-delegate** 变体（多一段"可委派的只读审计子代理"，8368 字符），并在 `<项目>/.codex/agents/` 生成四个只读角色文件（space_group / chemistry / density / validation，来自 `crystalpilot/workbench/agent_roles/*.toml`），每个角色自带 `[mcp_servers.crystalpilot]` 覆盖：同一解释器与模块、`--project <本项目>`、`CRYSTALPILOT_MCP_READONLY=1`。档位或策略一变就重写模板并写入/删除角色文件（带 `# crystalpilot-role-v1` 标记，用户自己的文件不动），对下一个对话生效；低档位或 off 时项目里没有任何角色，agent 无法按名拉起子代理。**探针（`workdir/r7-probe/run2.log`，一次性项目）**：`spawn_agent(agent_type="validation_ro", fork_turns="none")` 经 app-server 链路成功拉起项目级角色（默认的 full-history fork 会拒绝 agent_type，模板里写明）；子代理得到**自己的** MCP 进程（`mcp_server.jsonl`：pid 55932 `readonly: true`，父进程 pid 56808 `readonly: false`），其 `edit_atoms` 被服务端拒绝（"blocked: this workbench is in READ-ONLY mode"）。第一次探针还证明：没有 MCP 覆盖的个人级角色也会得到自己的一份 MCP 进程，**可写**：所以个人级 `codex-home/agents/` 一律不放角色。UI：权限菜单"只读审计子代理（最高档提示）"开关 + 当前状态行。战役清单 `defaults.subagents` 可写 off 做对照 |
| R7 一格对照（2026-09-05） | reg11-cage（委派了 2 次，死于两次 `stream disconnected` + 主机崩溃，no_delivery）→ reg12-cage（委派 3 次：chemistry + density 在元素/密度分叉，validation 在交付前抓到 VALIDATION.md 三处事实错误；全部只读；主 agent 每次用自己的 ghost_test 对照后再动手）= **below_bar**，R1 0.1986 @ 1.0 Å 截断、组成丢 N/Cl、树里有更好节点；基线 reg2-mof cage below_bar R1 0.157。墙钟 62.7 vs 72.5 min，父线程 tokens 29.0 M vs 23.7 M。**结论：机制可用，未证明有益；默认策略不放宽**。详见 `docs/reg1-2026-09-04/SUBAGENT-TRIAL.md` |
| R3-R7 策略统一（2026-09-06） | `subagents = top_tier（默认）/ aggressive / off` × 有效档位 → 委派层级 `off / hint / aggressive`（`agent_roles.delegation_tier`）：top_tier 在 xhigh 及以上给提示变体；aggressive 在 **max / ultra** 换主动委派变体（v40-aggressive：三个固定检查点，采纳空间群前 / 骨架建成掩膜前 / 交付前，每份判词一张裁决表，一个检查点总墙钟 15 分钟上限），在 xhigh 同 top_tier；五个只读角色（新增 `refinement_strategy`）与嵌套 `consult_specialist` 共用 `workbench/subagent_contract.py` 的专长表与判词 schema（`parse_verdict` 不补造字段）；设置字典 `delegation.tier / aggressive_efforts`；UI 三选一（最高档提示（默认）/ 最高档主动委派 / 关闭）；`run_live.py --subagents aggressive`。**子线程 token 不可观测**（Codex `collab_*` 事件的 `agents_states` 为空），不伪造。三个检查点是模板约定，没有服务端强制。**A/B 四格待 API 恢复**（协议 `docs/reg1-2026-09-04/R3-R7-draft.md` §4） |

## 6. 前端工作台

| 项 | 状态 |
|---|---|
| 构建指纹 | 有 R0 新增：`/api/health.ui_build`（构建时间、构建时 sha、脏标记、当前 HEAD、最新源文件、`stale`、`head_mismatch`）；权限菜单页脚显示；Playwright 守卫 |
| 主题 | 有 2026-09-05 复核修正：浅/深暖中性色；文本角色对 bg/surface/**raised** 以及小字号实色按钮文字均要求 ≥ **4.5:1**（`theme.test.ts`）；这是令牌组合守卫，不宣称所有透明度/图层组合均已获完整无障碍认证。**R3-R2-B**：浅色 `ink-2/ink-3` 加深一档（#504b44 / #605a52）、深色 `ink-3` 提亮（#a7a297），守卫改为 ink-2/ink-3 在三种底色上 ≥ **5:1**；`ShellCurve` 的 8–10.5 px 硬编码字号改用 11 px 下限令牌 |
| 字号 | 有 2026-09-05 复核修正：13/14/15/16 px 档位仍保留，最小文字用 `max(11px, …)` 防止 13px 档把下限缩成约 10.2px；真实 300px 侧栏 × 两主题 × 13/16px 回归通过 |
| 右栏"结构"页 | 有 R1：画布为唯一 flex 子元素（结构是主体，D4 关闭）；控件浮在画布上，左上范围/生长菜单 + ＋生长，底部 绘制/关系/证据/视图 面板条 + 带图提问/导出/重置图标；结构卡折叠为两行关键参数条（R1 wR2 GooF · 式量 · 原子/键数），按需展开 |
| 生长 / 范围 | 有 R1：见 §2 切片种类；菜单每项标注对应的 Olex2 命令；超胞项显式切模式（原先 `setSuperN` 在默认边长下是空操作）。**R3-R2-B**：菜单分两组并加组标题，范围（非对称单元 / 晶胞 / 超胞 2³ 3³ 4³ / 半径 8、16 Å / 分数盒）与动作（长一层 / 扩展至 4 层 / 补全 / 收回 / 装配）；Olex2 的 Mode Grow（短接触 / vdW / 选中原子的对称像）与"长满直到算符重复"仍未做（R5，需服务端候选键） |
| 回合摘要 | 有 R1：`validate_structure` 的评分 `breakdown.r1` 不再被当成 R1（D20）；指标游标只随改模型工具前进；`CRYSTAL_MUTATING` 与服务端 `MUTATING_TOOLS` 由 `tests/test_ui_tool_sets.py` 锁定（D21） |
| 工作动效 | 有 R1：呼吸星标 + 当前动作 shimmer + 回合计时；工具进度行（tool_progress）跟在标题后。**R3-R2-A**：统一为状态行 `StatusRail` 里的一条动作行（星标 + 当前工具人话 + 已用时 + 可插话），5 分钟后 shimmer 静音只留计时；行淡入 120 ms、字形交叉淡入 150 ms、阶段软脉冲 2.4 s，`prefers-reduced-motion` 全局静态 |
| 主会话栏（R3-R2-A，2026-09-05） | 有 三级信息层级：L1（用户/插话、Agent 说明、提问/审批/交付卡、回合分隔）全权重；L2 工具行单行（字形 · 人话标题 · 结果一句话 · 用时，失败行 3 px 左条 + 原因行内，展开才有芯片/告警/技术详情，原始 JSON 再深一层）；L3 原文按需。`StatusRail` 取代计数版阶段轨、头部徽章与浮动药丸（阶段 ✓/●/○，计数进 tooltip，窄屏横向滚动；动作行 / 等待批准 / 空闲 · 上次回合 mm:ss · 节点）；简洁模式隐藏自动通过的审批；交付卡只列状态芯片 / 豁免 / 节点 / R1 wR2 GooF / 五个主文件 / "全部 N 个文件 → 产物页签"（过滤命令输出与 transcript）；上滚后"回到最新"。失败原因仍是工具英文原话（R3 WP1 后中文化） |
| 相机与聚焦（R3-R2-B） | 有 `fitNow()` 只对原子取包围球（3Dmol `zoomTo` 空选择才把晶胞框/图层形状算进去），大胞 MOF 的 ASU 不再是画面中央一小团；选中原子卡"居中"（单原子 300 ms 动画），"重置视角"回全景；聚焦模式（右栏 ≥ 64% 窗宽、左栏变抽屉、`?focus=1` 进 URL、Esc 退出）。晶胞框仍常显（可关图层未做） |
| 身份栏（R3-R2-B） | 有 两行固定栅格：节点 · 分支（title 全名）· 空间群 · 引用 ｜ 化学式（title 全式）· R1/wR2/GooF · 原子/键/多面体（容器 < 480 px 时让位）；Playwright 断言两行不溢出、截断带 title |
| 节点树（R3-R2-B） | 有 `lib/nodeTree.ts`：分支头行可折叠、`diag/*` 家族默认收成一行、git 车道按可见行区间 + 分叉落点打包并复用（28 条分支 ≠ 28 列）、每节点 R1 来源（未精修写"沿用 nXXXX"）、三种标记（当前查看 / 已交付（结果目录 MANIFEST）/ R1 最优（不含诊断分支））；分支对比默认收起、不含诊断分支。`GET /api/wb/refine/nodes` 带 `deliveries`，limit 400 |
| 左栏与产物页（R3-R2-B） | 有 项目显示名（`settings.display_name` → `context.json` 标题 → 目录名，左栏悬停改名，所有列表同步）；最近项目把 `ui-import-*` 与手动隐藏的折进"更多 N"（浏览器本地偏好）；产物页按交付目录分组（组头 = 状态芯片 · 节点 · 文件数，主文件在前，"全部 N 个文件"展开，运行日志单独折叠，路径用 `/`） |
| 长会话稳定性（R3-R1，2026-09-05） | 有 按 `eid` 精确去重（512 条近期窗 + maxEid；无 eid 的旧事件退回 (kind,ts) 窗）；"加载更早的记录 · 还有 N 条"走服务端分页并从原始事件重建；被 300 块显示窗挤出的用户/插话气泡仍留在 DOM（"更早的输入"）；插话不再切段、折叠不跨 loud 行、时长按秒换算；命令卡"查看完整输出"取服务端文件；四个区域各自 ErrorBoundary（sidebar / chat / crystal / app，可"重新加载此区域"与"复制诊断"），`window.onerror`/`unhandledrejection` 一并上报；指标页 `n_params<0` 不再画成"参数 −1"，`d_min` 标"数据标称"并从 SHEL 卡给"工作截断"。证据：取证夹具回放（83 行 + 2 条 SSE 重放：无 eid 复现幽灵卡，有 eid 零新增）、真实 10 224 行线程的刷新/分页 Playwright（`workdir/ui-evidence/r3-r1/`） |
| 结构类别（R6，主人的要求） | 项目设置 `structure_class`（small_molecule / macrocycle / cage / framework / salt_cocrystal，或未声明）：右栏"分析"页顶部的类别条由用户选定，页面只展开该类别相关的分析区（框架 → 拓扑网/互穿/孔道；笼 → 有限片段的空腔/球形度/大环；大环 → 最大无弦环/π–π；小分子 → 相互作用/堆积；盐/共晶 → 阴离子位置/氢键），其余折叠可展开；系统按分析产物给出"建议类别"及依据（`suggestStructureClass`：维度 ≥1 → 框架；0-D 片段 ≥40 原子、球形度 ≥0.7 且有 ≥8 元环 → 笼；≥12 元环 → 大环；≥2 个 ≥4 原子片段 → 盐/共晶），用户确认才写入；服务端校验并持久化（`.crystalpilot-workbench.json`），`get_project_brief` 每次实时读出并附"用户先验、不是模型约束"的说明；无服务端时回退 localStorage |
| 引用到对话 | 原子/峰/量距/孔/结构/图片/相互作用/拓扑/片段/客体/checkCIF 均有引用路径。2026-09-05 补齐视图引用的**来源节点、对称像、峰坐标与单位**，孔汇总明确每胞；引用不能把未绑定来源的 checkCIF 报告假绑到当前节点。**R3-R6**：草稿里的 `[anchor …]` 在输入框上方显示为可移除的引用芯片（文本仍是唯一真值）；对话里的锚芯片可点击，查看该节点（历史视图 + 返回最新，不检出）并选中第一个原子（对称像优先，没画时退回 ASU 原子；等该节点的场景到了再选，不在旧场景里误选）。未做：只选第一个原子、不回填当时数值 |
| 提问卡（R3-R6） | 有 模板 v40 协议：Agent 缺一件会改变下一步的事实时写 ```` ```ask ```` 围栏（JSON：settled / dispute / question / options ≤4 / fallback）后结束回合；对话里渲染为静态卡，最新未答的一张停靠在输入框上方，按钮一键回复 `[prior] 问题：…\n回答：…`（运行中插话、空闲发送），"自己回答"把抬头放进输入框；"未答"= 最后一条 Agent 消息带卡且已流完、之后无用户消息也无更晚的 Agent 消息。非 JSON 围栏退化为只有问题的卡。没有服务端强制 |
| 插话回执（R3-R6） | 有 `user_steer` 之后立刻记 `steer_receipt {steer_eid, status: submitted \| failed, error}`（transcript + 直播；`POST /api/threads/steer` 返回 `receipt`）；气泡：发送中（乐观）→ 已记录（带 eid 到达）→ 已送达模型 / 未送达模型 + 重试（原话仍在 transcript）。回执只到"应用服务器接受了 turn/steer"为止 |
| 设置面板（R3-R6） | 有 权限芯片菜单新增 知识模式（完整 / 纯工具；运行中切换 409 → 提示）与 结构类别（服务端类别表 + 未声明） |
| 左栏 | 有 R1：项目行下有状态行（R1 · 节点数 · checkCIF A，30 s 轮询 `/api/projects/status`）；主题/字号/旧版界面进设置弹层；文案入 `zh.ts`；最近项目上限统一为 8（D12/D13 关闭） |
| 分析页签 | 右栏"分析"（R3.5）：对称唯一的相互作用表（六类，判据行、满足/共、分子内芯片、对称码、几何、引用）、孔道表（体积 · 维度 · 方向 · LCD · PLD ± · 内切球心/质心 · 电子）、堆积数字 ±、客体位置表（site 芯片、孔编号、宿主片段、间隙、最近接触、定义展开）；读 `GET /api/wb/refine/analysis`（按节点缓存 `analysis.json`，与 R5 的 `analyze_packing` 同一份产物）；R4 加"拓扑"区：独立网（维度、关系、互穿芯片、备注）、简化网（节点/边、连接数直方图、节点列表、RCSR 状态、可信度说明）、螺旋链（螺旋符号 · 手性 · 螺距）、有限片段（原子数 · 宿主/客体 · 份数 · 最大无弦环 · 球形度 · 纵横比 · 最长轴），定义可展开，网与片段可引用；查看器"关系"组新药丸"简化网"（节点球 + 带平移的边，读同一份产物，加载/为空/失败三种药丸） |
| 测试 | vitest 200（+ 配位表、相互作用文案、tiles、分析引用、拓扑/片段引用）；Playwright 5（真实项目、两主题；R2 起含孔道标签与关系图层截图，R3 起含分析页签，R4 起含拓扑区与简化网叠加层）；无组件渲染测试 |

## 7. 验证与证据（R0 基线，2026-09-04）

- pytest 有效基线 1710 passed / 1 skipped（+ `test_ui_build_info.py` 7）；已知负载抖动：olex2c 活体冒烟、shelxt staged 计时、superflip 金样。
- vitest 138；tsc 干净；Playwright 5（`docs/UI-EVIDENCE-2026-09.md`）。
- 真实项目在盘：hex、cage、dbu、nm 四个（`H:/CrystalPilot-campaigns/…`），只读使用。
- 交叉核对可用但未接入测试：PLATON（`run_checkcif` 已跑 `platon -u`）、Olex2 headless（`io/olex2c.py::Olex2Console.command()` 可发 `htab`/`pipi`/`calcVoid`）。

## 7b. 已知答案校验（2026-09-05，主人的要求：分析计算必须对）

两条独立副线各自拿**文献里已有答案**的真实结构直跑引擎，逐项对照；不一致的追到原因，属于代码缺陷的按元素通用规则修并配合成回归测试。报告：`docs/VALIDATION-2026-09-interactions.md`、`docs/VALIDATION-2026-09-topology-pores.md`；测试 `tests/test_known_answers_interactions.py`（含 `slow` 标记的 PLATON 交叉）、`tests/test_known_answers_topology.py`（37）；数据 `benchmark/known_answers/`（COD 记录 + SOURCES.md 逐条引文）。

| 对象 | 对照方 | 比了 | 一致 | 说明 |
|---|---|---|---|---|
| 氢键（D–H···A、对称码） | 14 个结构的存档 `_geom_hbond_*` | 78 行 | 76 | 最差偏差 D···A 0.0015 Å、H···A 0.009 Å、角 0.7°、**对称码错 0**；2 行是 H···F 距 Σr_vdW 差 6 / 15 mÅ 的边界例（存档用的判据比我们松），记为 KNOWN_GAPS 并每次重测 |
| 氢键 / π–π / X–H···Cg / 卤键接触 | PLATON `CALC INTRA/INTER`（.spf + NOMOVE 驱动） | 76 / 5 / 9 / 14 | 全部 | 分类差异（C–H···O 我们归 chx、金属杂环不算芳香、跨 PART 不算）逐条按我们公开的规则解释 |
| 溶剂可及体积 | PLATON `CALC SOLV` | 1 | 1.3 % | 探针/网格不同，只比量级 |
| 网符号 / 节点连接数 / 互穿 / 维度 | Systre 19.6.0（已装 `vendor/gavrog/`，Apache-2.0）+ 文献 | MOF-5 pcu、HKUST-1 tbo、ZIF-8 sod、UiO-66 fcu、双重互穿 MOF-5 两张 pcu（关系 = 空间群操作 -x,-y,-z）、NU-1000 csq、Zn3 二维 | 全部 | 连接数直方图与文献一致（Zn₄O 6-c、Cu₂ 4-c + btc 3-c、Zn 4-c、Zr₆ 12-c、Zr₆ 8-c） |
| LCD / PLD | CoRE MOF 2019（Zeo++ 高精度） | 5 个骨架 | 全部在 ±0.6 / ±0.8 Å 内 | 最差 LCD Δ 0.28 Å、PLD Δ 0.66 Å；MOF-5 用 Nature 402 原文的接触距离代入我们的半径得 14.97 / 7.80，实测 14.912 / 7.787 |

**修掉的缺陷（都是通用修法）**：① 穿过对称元素闭合的环（反演中心上的苯环）`find_rings` 看不见，π–π / C–H···π 表因此静默缺行（Zn2_dhtp 的 π–π 表整张为空而 PLATON 有 16 行），现在按拓扑 + 操作阶数普查这些环，`criteria.pipi/chpi.rings_closing_through_symmetry` 点名列出、`ring_note` 说明该结构缺哪些环（不另造第二个找环器）；② `write_cgd` 输出重边让 Gavrog 整文件报 `duplicate edge`（Mn3 棒状 MOF 拿不到符号），边集合去重，`n_parallel_edges` 如实报；③ Systre 的报错与多分量结果被压成一句，`rcsr_symbols`（每个连通分量一个）、`systre_error`、"不在 RCSR 档案"作为答案而非失败（分析产物 v5）；④ `inscribed_sphere` 的 `pts[::k]` 只沿 z 抽稀，更细的网格反而给更小的 LCD（UiO-66 8.46 → 8.22 Å），改成分数晶格分箱 + 1-Lipschitz 精化，结果是格点上的精确最大值（voids v5，`lcd_exact`）；⑤ 三个测试把"没装 Systre"和 `VOIDS_CACHE_V == 4` 写死，改成契约断言。

**仍未验证 / 能力空洞**（2026-09-06 R3-R5 更新）：~~阴离子–π 没有独立答案~~ → COD 2232050（Cg···F 3.178 / 3.654 Å 同算符重量一致，π–π d_cc 3.3362 / 垂距 3.3331 / 滑移角 2.466°）；~~π–π 样本只有 5 行~~ → 18 行比对（Zn2_dhtp 的 12 行因对称闭合环建出而进入比对）；~~螺旋链没有真实晶体对照~~ → COD 4115425（6₁ 右手，螺距 41.608 Å，对称推论）；卤键仍只验了 I；纯 CIF 结构项目（R3 起可读）几何孔道路径已带 PLD 与沿轴 PLD，电子数保持 unknown；互穿从“未验证”变为环穿越判定（§1）。

## 8. 变更记录

### 2026-09-07 前端/内核对齐（tag 待打）
- **内核升级**：codex 升到官方最新 **npm @openai/codex 0.153.4**（`vendor/codex`，git-ignored；`scripts/update_codex_kernel.ps1` 安装/更新）。`crystalpilot/workbench/kernel.py` 自动选最新的 npm 构建（`CRYSTALPILOT_CODEX_BIN` 可覆盖 → vendor/codex 下最新 → pip），改动需重启服务器。pip SDK 仍 0.147。**模型目录改为生成**：`model_catalog.py` 从运行中的 codex 二进制抽取内置目录（缓存到 `workdir/cache/`）＝内置 + `codex-home/model_catalog.custom.json`，写到 `codex-home/model_catalog.json`（后者不再入库，只跟踪 custom 源与 `base_instructions.generic.txt`）；这样内核升级时内置 GPT 梯子/提示自动跟随。0.153 不认线程级 `config`，改用**进程级** `--config features.multi_agent/tools.view_image/model_context_window/model_auto_compact_token_limit`，标志变化时重建引擎（空闲即时、回合中回合末）。
- **子代理开关变"真开关"**：UI 的「是否开启子代理？」= `subagents ∈ {auto,on,off}`（旧 top_tier→auto、aggressive→on），经进程级 `features.multi_agent` 真正决定内核是否提供子代理工具；on ⇒ 角色文件 + 主动委派模板；auto ⇒ 有效档==该模型梯子最高档才开。
- **前端**（`ui/`）：模型/档位/提供方移出权限浮层，进**模型按钮**浮层（提供方标签页、按所连 API 自适应的模型列表与该模型自有档位、视觉/上下文徽章、自定义 id、切提供方给分叉/新建）；**自动 浮层**瘦身为四权限模式 + IUCr + 单个子代理开关 + 知识模式 + 结构类别，长说明进悬停 tooltip；新增**设置面板**（提供方与密钥/全局默认/上下文/外观/高级内核/关于），密钥只写 `secrets/<id>.txt`，永不回显/入库/打日志；**`/` 命令面板**（↑↓/Tab/回车/Esc，命令镜像 Codex CLI，`/status` 等把内核状态写成对话卡片）；上下文环带悬停卡（已用/剩余/阈值，压缩时转圈）；「正在压缩上下文」状态；项目 feed（SSE）+ config.toml 看门狗让 UI/CLI/配置文件实时同步；侧栏对话行悬停可重命名（同步内核）；打开项目对话框加**浏览…**（系统文件夹选择器）；项目主页写明"新对话将创建在 …"。切换对话的连接加固（事件流带 project、服务端按需登记线程）。
- **后端**（`crystalpilot/workbench/`）：新 `kernel.py`/`codex_config.py`（外科式改 config.toml，保注释、tomllib 校验、.bak、原子替换）/`model_catalog.py`/`providers.py`（密钥进 secrets/，auth 走 `print_token.py --cred`）/`models.py`/`routes_config.py`（`/api/kernel /providers /models /config /threads/{compact,rename,fork} /skills /system/pick_folder`）。`core.py` 加进程级引擎覆盖 + 通知 tap（压缩/线程状态/告警）；`service.py` 子代理三态语义 + 引擎重建/推迟 + 每次改动推 `settings` 事件。凭据解析（`providers.parse_credential_text`）与 `print_token.py` 同规则（`Key: …` 行优先，否则整文件为裸密钥）。
- **验证**：vitest 346 passed；`tsc` 0 error；`npm run build` OK；Playwright `e2e/r4-settings.pw.ts` 3 passed（截图 `workdir/ui-evidence/fe-0907/`，含模型菜单/权限菜单/`/`面板/设置面板/上下文环，正文正则确认无 `sk-…` 泄漏）；全量 pytest 2551 passed / 26 skipped（唯一的 test_olex2_tool::test_help_boot 是并行负载下的老计时抖动，单跑该文件 8 passed）。实机重启后 `/api/models?provider=crystalpilot` 回 22 个模型、`remote_available` 标注正确。
- **交付前设计复核（主人要求：简约、有高级感、只显示关键信息）**：`/` 命令面板不再重复打印与命令同名的 Codex CLI 提示（13 行里 8 行原本写成「/model … /model」）；模型行只在**有话可说**时给标记（视觉；内核未登记的给「未登记」），去掉了每行都有的 视觉/纯文本 徽章与目录圆点；提供方卡片标题用短名（全名进悬停）、次级操作改为无边框文字按钮；模型按钮与档位标题用短名（去掉括号后缀）；自定义 id 行去标签去边框；内核面板改中文标签（版本/来源/路径）；打开模型菜单时把**正在使用的模型滚动到可见**（原本在长列表底部被截断）。
- **主人报的崩溃已修 + 侧栏文件夹可折叠**：在项目 A 的对话里点项目 B，晶体窗格被 `OffscreenCanvas.transferToImageBitmap` 失败打死（Firefox）。根因：3Dmol 用**全页面单例**离屏画布与 WebGL2 上下文（`WebGL/Renderer.ts` 的 `_offscreen_singleton`），每帧前把它 resize 成当前视图画布的尺寸；换项目会重挂 CrystalViewer（`CrystalProvider` 以项目为 key），正在拆的那个视图把共享画布留成 0×0，下一帧即抛。现在：容器无尺寸不画、单帧失败只重试一次并记 `/api/ui/diagnostics`，绝不掀掉窗格。同时**点项目文件夹改为就地展开/折叠该项目的对话**（非当前项目的对话首次展开时才拉取，折叠状态记住），切项目只在点某条对话或悬停「打开」时发生；两条路径都改成先 navigate、由 `ProjectUrlSync` 打开项目，避免出现「A 的对话挂在 B 项目下」的中间态。回归在 `e2e/r4-settings.pw.ts`（4 passed）。
- **主人首次实机使用时打出的 500（已修）**：在模型菜单里点过 Claude / GPT-5.1 / GLM-5.3-Flash 之后，**任何项目**发消息都回 500。根因：`learn_model` 把 OpenRouter 报的 `input_modalities` 原样写进模型目录，而 OpenRouter 对 Anthropic/OpenAI 模型报 `file`、对 GLM-Flash 报 `video`，codex 只认 `text/image/audio` → `model_catalog.json` 整个解析失败 → codex 连配置都加载不了，`thread/start` 直接报 `unknown variant \`file\``。**一个只是被浏览过的模型就让整个系统不可用**，所以过滤放在每个条目必经的收口处（写入 custom.json + 渲染进目录），手改的 custom 文件也毒不了目录；已存的 9 条同时清洗。实机验证：用户项目里线程可以起来、MCP 挂上 74 个工具、回合正常完成。回归 `test_provider_modalities_are_filtered_to_what_codex_accepts`。
- **两个实测修复**：① `learn_model` 不再把已登记的自定义条目**改挂到另一个提供方**：在网关标签页学习 `z-ai/glm-5.3-flash` 曾把它的 `provider` 改成 crystalpilot，而 `list_models` 按这个标记过滤自定义条目，于是它从 OpenRouter 标签页里消失；现在条目保留首次学习时的归属，后续只升级事实（回归测试 `test_learn_model_never_moves_an_entry_between_providers`）。② 空的目录档位表表示「未知」而非「没有推理档位」，会继续查提供方元数据与回退梯子（此前 flash 的档位显示为「无」、不发 effort）。
- **服务器「凭空消失」的真凶（已修）**：主人报 8010 打不开。查下去是最难看的一种故障，**进程还活着、应用也正常，端口上却没有人在监听**。Windows 的 Proactor 事件循环碰上一次 accept 失败（`WinError 64`「指定的网络名不再可用」，客户端在 AcceptEx 完成前断开即可触发）后，asyncio 会顺手**把监听套接字关掉**（`proactor_events.py` 的 `_start_serving`：先报 `Accept failed on a socket`，再 `sock.close()`），而 uvicorn 的 `main_loop` 只看 `should_exit`，于是进程活得好好的、永远聋着：没有崩溃、没有退出码，健康检查也无从查起，唯一的痕迹是 `workdir/uvicorn_r13.err.log` 末尾那段 traceback。修法：`scripts/restart_server.ps1` 改用 `--loop asyncio:SelectorEventLoop`（uvicorn 会把它不认识的 `--loop` 值当作事件循环工厂的导入串），selector 路径把同一件事看成 `ConnectionAbortedError` 并继续监听。代价说清楚：本进程 select() 上限 512 个套接字（约为实际负载的 20 倍），且不能用 asyncio 子进程，我们一个都没有（codex SDK 用的是 `subprocess.Popen`）。回归 `tests/test_server_loop.py`（4 条：uvicorn 的自定义 loop 契约、结果不是 Proactor、脚本确实带这个参数、脚本杀进程用的命令行正则仍能匹配加了参数后的命令行）。2026-09-05 19:55 那次「死因未证实」的消失几乎可以肯定是同一件事。
- **顺带修掉一处静默降级**：服务器重启后，浏览器里已经开着的页面会在头几秒收到 `400 项目未打开` - EventSource 只在**网络**中断后自动重连，收到非 2xx 就永久放弃，于是页面看着一切正常，却不再收到 `settings` / 审批 / 改名 / 忙碌状态推送，要 F5 才恢复。`WorkbenchProvider` 的项目 feed 现在带重连梯子（1 s 起指数退避到 15 s，先重开项目再接回；网络抖动照旧交给 EventSource 自己重试）。回归 `ui/e2e/feed-reconnect.pw.ts`，拦截首次 feed 请求返 400，断言确实重试并重开了项目；**已确认把修复撤掉它就失败**。另修 `e2e/r2b-layout.pw.ts` 的一处错误断言：产物面板对「只写过 transcript」的线程本来就只有日志区、没有交付分组，旧断言无条件要求有分组，于是在机器上最新的线程是一条连通性检查时误报（不是产品缺陷）。
- **本次验证**：`tests/test_server_loop.py` 4 passed；服务器/提供方子集 64 passed；vitest 346 passed；`tsc` 0 error；`npm run build` OK；Playwright 全套 34 passed / 4 skipped（`r2b-layout` 6 条修正后全过）。实机端到端：在主人自己的项目 `usertest/test2-0907` 起线程 → MCP ready → 模型回「已连接」→ `turn_completed` 3.07 s。


| 日期 | 轮 | 变更 |
|---|---|---|
| 2026-09-04 | R0 | 重建并核对 `ui/dist`（D1 关闭）；新增构建指纹（服务端 + 权限菜单 + Playwright 守卫）；新增对比度守卫（KNOWN_DEBT 记录 D2 数值）；Playwright 证据套件；本文与 `UI-EVIDENCE` 建档；tag `r2-pre-r1` |
| 2026-09-04 | R1 | 色板/类型刻度/设置弹层（D2 D3 D13 D16）；左栏状态行（D12）；结构为主体 + 浮动工具条 + 两行关键参数条（D4）；Olex2 语义范围菜单，服务端新增 radius / range 切片与 grow -w（`build_scene` 新参数，缓存键仅在使用时扩展）；回合摘要误读评分修复（D20）+ 改模型工具集守卫（D21）；工作动效；tag `r2-r1-done` |
| 2026-09-04 | R2 | **一份成键真值** `chem/bonding.py` + 六步消费者迁移（每步一提交、现状照片测试记录移动的数字；D8 D9 D10 D18 关闭，`is_metal` 白名单）；相互作用引擎 `chem/interactions.py`（氢键 / π–π / C–H···π / C–H···X / 卤键 / 阴离子–π，边界规则、h_source、PART；D5 D6 D19 关闭）接入场景与查看器（六个图层药丸、点击看几何 + 判据 + 引用）；`range`/tiles 契约与叠加层随范围平铺（D7 关闭，`scene8_`）；孔道 v3：维度 / 方向 / 内切球 / LCD，贯穿孔质心停用并公告（D17 关闭，R3.0–3.1 提前）；`run_shelxl(extra_cards)` 白名单通道 + HTAB→`final.cif`（D11 关闭，R5 提前）；tag `r2-r2-done` |
| 2026-09-04 | R3 | 分析产物 `refine/analysis.py`（按节点缓存，对称唯一相互作用表 + 孔道 + 堆积 + 客体，同一份给页签 / `analyze_packing` / HTAB）与右栏"分析"页签（可引用）；相互作用行带分子内/分子间；`chem/pores.py` 距离场 + **PLD 渗流二分**（取遍所有路径；计划的 6.6 Å 期望改正为 10.592）+ 堆积数字（并集堆积指数 ±、Å³/非氢原子；直接求和只作对照），voids.json v4；`chem/guests.py` 宿主掩膜（只用宿主原子，无需反射数据）+ 客体归属（笼内 / 通道 / 分子间空腔 / 晶格间隙，定义随输出，按片段实例计壁面，宿主 ≥50% 规则）；查看器通道方向箭头、孔道标签带 PLD；tag `r2-r3-done` |
| 2026-09-04 | R4 | `chem/shape.py`（CShM 钉文献常数、最大无弦环、形状证据、晶体学点对称）与 `chem/topology.py`（独立网与互穿的对称相关判定、节点–连接子简化网 + 门控 Systre、螺旋链）两条副线并入；分析产物 v4 加 `topology` 块（网 / 简化网 / 螺旋 / 0-D 片段的大环 + 形状证据），"分析"页签加拓扑区，查看器加"简化网"叠加层，`inspect_model` 金属环境带 CShM，`analyze_packing` 加 `topology` 块；模板 v36；tag `r2-r4-done` |
| 2026-09-04/05 | R5 | `analyze_packing`（四块 + 拓扑，判据随表，HTAB 卡）；`get_geometry` 二面角 / 平面 / 质心 + CONF/MPLA 卡；每个工具描述末尾的参数摘要行；`situation_report` 的 `stage` 与只读堆积摘要；reg8 四项（候选已试/已裁决、导入即报重复 hkl、无原子晶胞视图、分裂后复制骑乘 H）；规格缓存内容指纹（D15）；一格端到端 reg9-dbu 首跑暴露 **P0：worker 线程首次 `import scipy.spatial` 的 loader-lock 死锁**（§4 导入纪律行；修复 + 冷进程回归已提交，复跑 reg10-dbu（2026-09-05 00:44–01:13）：publication，R1 0.0463 / Δ 0.0016，组成与参考相同，`analyze_packing` 两次各 1.4 s 返回，`HTAB N2 O1` 经 `run_shelxl(extra_cards)` 进 `final.cif`（带 esd 的 `_geom_hbond_` 行，D11 首次在真实交付里走通）；已知答案校验两条副线并入（§7b）；tag `r2-r5-done` |
| 2026-09-05 | R7 | 子代理：Codex 0.147 自定义角色调研 + 两次机制探针（项目级角色经 app-server 可拉起、需 `fork_turns="none"`、子代理另起 MCP 进程、角色自带只读覆盖才只读）；落地 `workbench/agent_roles.py`（按项目写/删四个只读角色）、模板 v37 + delegate 变体（只在 xhigh × top_tier 渲染）、设置 `subagents`/`delegation`、UI 开关/状态行/系统行；runner 记录 delegate 变体并对瞬时流中断重试；通知归一化把 custom tool 形式的 spawn/wait 变成 `collab_*` 事件、未知条目留名；一格对照 reg11→reg12（§5）；tag `r2-r7-done` |
| 2026-09-05 | R3-R0 | 受管重启脚本 `scripts/restart_server.ps1`（按命令行识别 uvicorn、PID 文件、固定日志）、参数化实机启动器 `scripts/run_live.py`、档位探针（max/ultra 可用 → 六档）、计划 `docs/PLAN-2026-09-05-round3.md`；基线 tag `r3-pre`（全量 pytest 的 basetemp 必须放在仓库外，否则结构文档项目测试整体报错） |
| 2026-09-05 | R3-R1 | **并发冷启动修复**：Codex 在规格缓存冷时会为同一线程同秒起两个 crystalpilot MCP 进程，二者同时导入起始模型并撞在共用的 `state.json.tmp` 上（WinError 32），其中一个整场只剩 `crystalpilot_error`，第一格 org_hsl 实机就死于此；修法：按进程唯一临时名 + 重试的原子写、`bootstrap.lock` 保证 n0000 只提交一次、MCP 打开失败重试三次并把异常写进 `mcp_server.jsonl`（`tests/test_concurrent_open.py`，含三进程同刻打开真实合成项目） |
| 2026-09-05 | R3-R1 | 事件身份 + transcript 分页 + generation 感知重连 + 命令完整输出 + 浏览器诊断端点（服务端）；eid 去重/分页重建/更早输入常驻/ErrorBoundary/指标标注（前端）；取证夹具进 vitest；真实长线程 Playwright 刷新用例；一格小分子实机（见 §7c） |
| 2026-09-05 | R3-R2-A | MCP：codex 的 `resources/templates/list` / `prompts/list` 探针返回空表（原先 -32601 让 agent 以为工具没了）；新建/续接会话首回合等待工具注册（75 s / 未列出 20 s），`mcp_startup` 系统行落 transcript。主会话栏：三级信息层级、`StatusRail`、工具行单行化、简洁模式隐藏自动审批、交付卡、回到最新、动效目录；vitest 295、Playwright r3-r2a 13 passed |
| 2026-09-05 | R3-R2-B | 结构为主体：相机只框原子 + 居中、范围菜单分 范围/动作、ink-2/ink-3 ≥ 5:1、左栏显示名/改名/"更多"、身份栏两行、节点树折叠/车道打包/沿用/已交付/最优、聚焦模式、产物页按交付分组；服务端 `deliveries` 与 `display_name`；vitest 312、`r2b-layout.pw.ts` 6 用例 × 3 个真实项目（小分子 / Zr-MOF 225 节点 / Zr6 笼 189 节点）全过；全量 pytest 2368 passed / 25 skipped / 0 failed（2026-09-05 23:07，10 min 54 s，仓库外 basetemp，`workdir/pytest-full-r3r2.log`；R1 时 2355）；tag `r3-r2-done` |
| 2026-09-05/06 | R3-R3 | 工具面 P0：`tool_status` 分层结果信封（`tools/base.py::invoke` + `project.invoke_tool` 前后节点；UI 芯片）；诊断删除绕过幽灵账本处置但记 `diagnostic_touches`（WP6）；`situation_report` 掩膜块读 `solvent_mask_info`（T-k）；schema 边界守卫 + 首批 7 处 minimum/maximum、知识卡与掩膜诊断改条件句、配位客体措辞（WP8 / T-j）；PART 生命周期 `refine/parts.py` + `edit_atoms(set_part/clear_part)` + `restraints.preflight()` + 第 71 个工具 `preflight_restraints`（WP3）；客体证据记账逐原子 / 分母带标签 / 循环与终止原因 / `conditioned_by_prior`（WP4）；有效模型状态 `effective_cards` / `node.json.effective_state`（schema 2）/ model.res 携卡 / checkout 还原 / 作业自动带入 / `write_outputs.restartable`（WP2）；实机一格 DBU 盐；tag `r3-r3-done` |
| 2026-09-06 | 提供方 | 网关断流后主人给了 OpenRouter 临时渠道：`[model_providers.openrouter]` + `print_token.py --cred` + `model_provider_override`（新线程生效）+ `run_live.py --provider`（`cd78a03`）；按提供方的推理阶梯（GLM 用 high；high 同时是顶档与 aggressive 档）、`settings.vision` + `CRYSTALPILOT_NO_IMAGES`（文本模型不收图，图片留盘）、MCP 参数 JSON 字符串解码（`9fa9d81`）；首格 r6-mof-ask-glm 死于 view_structure 图片（404），复跑 r6-mof-ask-glm2 无视觉；测试 `tests/test_model_provider_setting.py`（11）、`tests/test_model_caps.py`（5） |
| 2026-09-06 | 运维 | **CPU 上限从未生效**：`procutil.limit_cpu` 没给 ctypes 签名，`GetCurrentProcess()` 伪句柄被截成 32 位，SetPriorityClass / SetProcessAffinityMask 静默失败却打印"cpu-limited to 4 core(s)"；R7 A2 格实测 MCP 进程 30 s 占 20.3 核、Normal 优先级。修后（3913161）声明签名、检查返回、`cpu_state()` 回读并把真实结果写进提示，MCP 服务进程自己也调一次；实测服务器与 MCP 子进程 affinity=15、BelowNormal（`tests/test_cpu_cap.py` 起子进程回读）。09-06 15:19 之前的所有实机格都是无上限跑的 |
| 2026-09-06 | R3-R7 子代理 | A2 格实证两处缺陷：① codex 对未知（第三方）父模型不继承，子代理跑在 codex 默认 gpt-5.5 上；② 子代理首回合在自己的 MCP 服务器 tools/list 之前发出，整回合无工具。修法（b78436d）：角色文件写 `model = effective_model(settings)`；委派模板 v41 两步走（spawn_agent(message="回复 READY") → wait_agent → send_input 任务 → wait_agent） |
| 2026-09-06 | R3-R7 子代理·模型目录 | 续接线程实证第三处缺陷：角色文件带上 `model` 后，codex 的 spawn_agent 用模型元数据校验角色的 reasoning effort，未知（第三方）模型的元数据是空表 → "Reasoning effort `medium` is not supported for model `z-ai/glm-5.3-flash`. Supported reasoning efforts: （空）"，三个角色全部被拒。修法：`codex-home/model_catalog.json`（config 键 `model_catalog_json`，从 codex 自带目录的 gpt-5.5 条目改出）声明 z-ai/glm-5.3（text，low/medium/high）、z-ai/glm-5.3-flash（text+image）与 gpt-6-astra（low…ultra），base_instructions 用 codex 原本给这些模型的通用提示词（提示词不变）。探针 `_probe/glm53f2`：spawn_agent 描述列出三个模型并写明"子代理默认继承父模型"，两步委派拿到 READY 与真实 get_project_brief 判词。另：`model_override` 变更现在会重渲染角色文件（之前一次性设置里 subagents 先于 model 处理，角色钉在了 config 默认模型上） |
| 2026-09-06 | 审批 | **auto 模式无人值守**：命中保护区黑名单（`.codex`、codex-home、testAPI、E: 盘、taskkill…）的 shell/文件审批不再排队等人（A2 续接线程两次因写 `.codex\tmp` 脚本各卡 10 min），改为立即拒绝并在 `approval_decision` 事件里写明 `policy=shell_blocklist_reject`、`blocked_by=<模式>`，模型自行改道；普通命令照旧自动放行，copilot 等模式照旧排队（d8d3df4，`tests/test_approval_policy.py`） |
| 2026-09-06 | R3-R7 子代理·热身 | 第四处：父模型（Flash）不按两步协议、一次性 spawn+wait 时子代理首回合仍无工具。实证 codex 在同一回合每次工具往返后重建工具表（A2′ 子代理第二个请求 mcp=1），所以五个角色文件开头加"先调一次 list_mcp_resources 再审计"（7510458）。R7 GLM A/B 结论：机制可用、无科学收益、默认 top_tier 不放宽；cage 成败由提问卡决定（A1 问了→Zr6 核心；A2′ 没问→骨架未闭合） |
| 2026-09-06 | R3-R7 | 子代理分档：策略 off / top_tier / aggressive → 层级 off / hint / aggressive（`delegation_tier`），aggressive 模板变体（v40-aggressive）只在 max/ultra，五角色 + 共享判词契约 `subagent_contract.py`，UI 三选一，`run_live.py --subagents`；**顺带修复**：反常散射项随每次会话重建走（`apply_anomalous_terms`），重算掩膜改 `BypassMask` 并记 `bypass` / `anomalous_terms`（`VOIDS_CACHE_V=7`），"2.02 倍"电子数解释并修复（R3-R5 复盘 §7）；全量 pytest 2509 passed / 26 skipped / 0 failed（10:48）；Playwright r3-r7（设置三选一）+ r3-anom（分析页两数并列）；**A/B 四格与 R6 交互格待 API**；复盘 `docs/reg1-2026-09-04/R3-R7-draft.md`；tag `r3-r7-impl` |
| 2026-09-06 | R3-R6 | 人机交互：提问卡协议（模板 v40 "问人"节 + 对话卡 + 停靠卡一键 `[prior]` 回复）；插话回执三态（`steer_receipt` 事件、气泡状态、重试）；引用芯片（草稿可移除）与锚跳转（历史视图 + 原子选中，reducer 记场景节点）；设置面板知识模式 / 结构类别；vitest 342、`tests/test_steer_receipt.py`；Playwright r3-r6（合成尾巴 + 拦截发送，2 passed）；**交互实验一格被 API 断流挡住（三次预热失败），待复跑**；复盘 `docs/reg1-2026-09-04/R3-R6-draft.md`；tag `r3-r6-done` |
| 2026-09-06 | R3-R5 | 师兄功能正确性：对称闭合环真正建出（Zn2_dhtp π–π 12 行与 PLATON 逐行一致）；Olex2 `grow` 长满（`_grow_all`，查看器“长满”开关 + 截断药丸）；沿 a / b / c 的 PLD（`pld_along`，`VOIDS_CACHE_V=6`）与两个电子数口径（`electron_count_basis`）；环穿越互穿判定（`chem/threading.py`，`interpenetrated` true/false/未判定）；已知答案扩充（COD 2232050 阴离子–π + π–π，COD 4115425 6₁ 螺旋链，PLATON π–π 最少比对行数）；Playwright r3-r5（笼项目 6 + `r5-analysis.pw.ts` 2×2）；复盘 `docs/reg1-2026-09-04/R3-R5-draft.md`；tag `r3-r5-done` |
| 2026-09-06 | R3-R4 | 整片段姿态搜索 `search_fragment_pose` + 组级接受 `accept_fragment_pose`（WP5，`refine/tools_pose.py`）；试验账本 `refine/trial_ledger.py`（`project.invoke_tool` 钩子，`tool_status.prior_trial`）；研究目标 `refine/investigation.py` + `set_investigation` + diagnostic 交付门（WP7）；AGENTS v39（目标分级与停止规则；正文预算贴边 8981/9000）；实机证据反哺：客体检验 3 位移对照、锚点优先排序；同题复跑 Zr-MOF 一格（无人插话，43 min / 123 次调用 / 117 节点，诊断交付 n0109 R1 0.0860，四条否决 + 六个未试方向进 open_items）；n0079/n0116 只读验证；复盘 `docs/reg1-2026-09-04/R3-R4-draft.md`；tag `r3-r4-done` |
| 2026-09-05 | R6 | 对话层：阶段轨（工具名映射 + `situation_report.stage` 校正）、回合收束行、交付摘要卡（只信工具结果）、审批卡四面（做什么 / 为什么 / 风险 / 改哪些文件）、引用锚 `[anchor node=… atoms=…]` + 芯片渲染、子代理目录（collab_* 事件）；结构类别（用户选定 + 系统建议，按类别展开分析区，服务端持久化，`get_project_brief` 读出）；两轮视觉自审（Playwright 三种窗宽 + 深色，修 8 处）；响应式右栏与窄屏抽屉；全量 pytest 2049/2；tag `r2-r6-done` |
