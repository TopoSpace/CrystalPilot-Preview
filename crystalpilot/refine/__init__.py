"""Agentic refinement workbench core: session, node store, typed tools.

This package turns the one-shot solve engine into a persistent, branchable
refinement session driven by typed tools (exposed to the Codex agent through
the MCP server in crystalpilot.mcp).
"""
